<!--ユーザー一覧-->

<% @users.each do |user| %>
<div>
  <!--プロフィール画像-->
  <p>image
    <%= image_tag user.get_profile_image(100, 100) %>
  </p>
　<!--ユーザー名-->
  <p>name<%= user.name %></p>
  <!--showページ(ユーザー)へのリンク-->
  <%= link_to "Show", user_path(user) %>
</div>
<% end %>